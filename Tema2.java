public class Tema2 {
// insert a new node - click on empty area
// select the node - click over a node (colour is changed from black to magenta)
// unselect a selected node - click on empty area
// with a node selected:
//             - left click on that node - delete the node (if that node is not connected - a connected node can not be deleted)
//             - right click on that node - rename the node
//             - left click on the other node - add/delete the edge between that nodes
//             - right click on the other node - rename the edge between that nodes
	
	public static int RADIUS = 15;                                                           // radius of a node (represented as a circle)
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		GUI_Tema2.Run();
	}
	
}